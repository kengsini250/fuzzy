#ifndef FUZZYMATH_H
#define FUZZYMATH_H

#define fuzzySize 11
extern float x2D[fuzzySize];

extern float BaseS[fuzzySize];
extern float BaseM[fuzzySize];
extern float BaseL[fuzzySize];

extern float max(float f1,float f2,float f3);

#endif // FUZZYMATH_H
